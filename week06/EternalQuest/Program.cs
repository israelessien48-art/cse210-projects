using System;

class Program
{
    static void Main(string[] args)
    {
        // ✅ EXCEEDING REQUIREMENTS NOTICE
        // I added GOAL CATEGORIES as a creative enhancement.
        // Categories help organize goals (Spiritual, Health, Work, Personal, etc.).
        // Categories are displayed, saved, loaded, and selectable during goal creation.
        // This improves organization and adds real-life usefulness to the system.

        GoalManager manager = new GoalManager();
        manager.Start();
    }
}
